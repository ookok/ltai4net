# LTAI Live Trace Report

| Item | Value |
|------|-------|
| **Query** | 写一个C#高并发LRU缓存 |
| **Time** | 2026-05-24 04:20:07 UTC |
| **Duration** | 81011ms |
| **L1** | deepseek/deepseek-v4-flash |
| **L2** | deepseek/deepseek-v4-pro |
| **L0** | local/bge-large-zh-v1.5-onnx |
| **ONNX** | enabled |

## Response

Here is a high-concurrency LRU cache implementation in C#:

```csharp
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Threading;

public class ConcurrentLruCache<TKey, TValue>
{
    private readonly int _capacity;
    private readonly ConcurrentDictionary<TKey, LinkedListNode<CacheItem>> _dictionary;
    private readonly LinkedList<CacheItem> _lruList;
    private readonly object _lock = new object();

    private class CacheItem
    {
        public TKey Key;
        public TValue Value;
    }

    public ConcurrentLruCache(int capacity)
    {
        if (capacity <= 0)
            throw new ArgumentOutOfRangeException(nameof(capacity));
        _capacity = capacity;
        _dictionary = new ConcurrentDictionary<TKey, LinkedListNode<CacheItem>>();
        _lruList = new LinkedList<CacheItem>();
    }

    public bool TryGet(TKey key, out TValue value)
    {
        if (_dictionary.TryGetValue(key, out var node))
        {
            lock (_lock)
            {
                // Re-check that node is still in list (could have been evicted)
                if (node.List != null)
                {
                    _lruList.Remove(node);
                    _lruList.AddLast(node);
                    value = node.Value.Value;
                    return true;
                }
            }
        }
        value = default;
        return false;
    }

    public void Put(TKey key, TValue value)
    {
        lock (_lock)
        {
            if (_dictionary.TryGetValue(key, out var existingNode))
            {
                existingNode.Value.Value = value;
                _lruList.Remove(existingNode);
                _lruList.AddLast(existingNode);
            }
            else
            {
                if (_dictionary.Count >= _capacity)
                {
                    var lruNode = _lruList.First;
                    if (lruNode != null)
                    {
                        _lruList.RemoveFirst();
                        _dictionary.TryRemove(lruNode.Value.Key, out _);
                    }
                }
                var newItem = new CacheItem { Key = key, Value = value };
                var newNode = new LinkedListNode<CacheItem>(newItem);
                _lruList.AddLast(newNode);
                _dictionary.TryAdd(key, newNode);
            }
        }
    }

    public int Count => _dictionary.Count;
    public int Capacity => _capacity;
}
```

This implementation provides thread safety by combining a lock-free dictionary for lookups with a single lock around the ordered list. On each successful Get, the accessed node is moved to the end (most recently used) under the lock. The Put method always acquires the lock to ensure atomic updates. The ConcurrentDictionary allows non-blocking reads for misses. To further increase concurrency, you could shard the list lock or use an approximate LRU algorithm like segmented LRU or a clock-based eviction. For production, consider using `Microsoft.Extensions.Caching.Memory.MemoryCache` with `MemoryCacheEntryOptions` for high-performance, built-in expiration and sizing.

---
*Generated at 2026-05-24 04:20:07 UTC*
